<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html,
        body {
            height: 100%;
            width: 100%;
            font-family: poppins;
            color: #ffffff;
        }

        #main {
            background-color: rgb(255, 255, 255);
            height: 100%;
            width: 100%;
        }

        #main .users {
            display: flex;
            flex-wrap: wrap;
            overflow: hidden;
            padding: 10px 10px;
            gap: 10px;
        }

        #main .users .user {
            flex-shrink: 0;
            max-height: 40vh;
            border: 4px solid rgb(0, 0, 0);
            border-radius: 10px;
            overflow: hidden;
            background-color: rgb(0, 0, 0);
            padding: 10px 10px 10px;
            text-decoration: none;
            cursor: pointer;
            color: rgb(255, 255, 255);
        }

        #main .users .user img {
            height: 25vh;
            width: 15vw;
            object-fit: cover;
        }
    </style>
</head>

<body>
    <div id="main">
        <h2> This is Feed of
            <%= loggedInUser.username %>
        </h2>
        <div class="users">
            <% users.forEach(user => { %>
                <a href="/like/<%=  user._id %>/<%= loggedInUser.username  %>" class="user">

                    <img src="<%= user.pic %>" alt="">
                    <h4>
                        <%=user.username%>
                    </h4>
                    <p>
                        <%=user.fullName%>
                    </p>
                    <h4>Likes :
                        <%= user.likes.length%>
                    </h4>
                    </a>
                <%   }); %>
        </div>
    </div>
</body>

</html>